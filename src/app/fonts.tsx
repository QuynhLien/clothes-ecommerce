import { Inter, Lora, Pinyon_Script } from "next/font/google";

export const inter = Inter({
  subsets: ["latin"],
  display: "swap",
});

export const lora = Lora({
  subsets: ["latin"],
  display: "swap",
});

export const pinyon_script = Pinyon_Script({
  subsets: ["latin"],
  display: "swap",
  weight: "400",
});
